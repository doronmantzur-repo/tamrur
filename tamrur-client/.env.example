# Base URL of the tamrur-server API this client talks to.
# Local dev (npm run dev) uses .env.development, production build (npm run build)
# uses .env.production - both already set in this repo, so this file is just documentation.
VITE_API_URL=
